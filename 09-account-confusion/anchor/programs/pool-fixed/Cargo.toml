[package]
name = "pool-fixed"
version = "0.1.0"
description = "Account Confusion Fixed Demo"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "pool_fixed"

[features]
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.32.1"
